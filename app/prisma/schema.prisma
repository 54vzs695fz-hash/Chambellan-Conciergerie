generator client {
  provider = "prisma-client"
  output   = "../src/generated/prisma"
}

datasource db {
  provider = "postgresql"
}

model Client {
  id          Int      @id @default(autoincrement())
  full_name   String   @default("")
  phone       String   @default("")
  whatsapp    String   @default("")
  email       String   @default("")
  nationality String   @default("")
  notes       String   @default("")
  preferences String   @default("")
  created_at  DateTime @default(now())
  updated_at  DateTime @updatedAt
  trips       Trip[]
  relationships         ClientRelationship[] @relation("ClientRelationships")
  related_relationships ClientRelationship[] @relation("RelatedClientRelationships")

  @@map("clients")
}

model ClientRelationship {
  id                Int      @id @default(autoincrement())
  client_id         Int
  related_client_id Int
  relationship_type String   @default("")
  notes             String   @default("")
  created_at        DateTime @default(now())
  updated_at        DateTime @updatedAt
  client            Client   @relation("ClientRelationships", fields: [client_id], references: [id], onDelete: Cascade)
  related_client    Client   @relation("RelatedClientRelationships", fields: [related_client_id], references: [id], onDelete: Cascade)

  @@unique([client_id, related_client_id])
  @@index([client_id], map: "client_relationships_client_id_idx")
  @@index([related_client_id], map: "client_relationships_related_client_id_idx")
  @@map("client_relationships")
}

model Trip {
  id                       Int       @id @default(autoincrement())
  client_id                Int?
  client_name              String    @default("")
  destination              String    @default("")
  multi_destination        Boolean   @default(false)
  destinations             Json      @default("[]")
  destination_region       String    @default("")
  arrival_date             String    @default("")
  departure_date           String    @default("")
  hotel                    String    @default("")
  villa                    String    @default("")
  driver                   String    @default("")
  butler                   String    @default("")
  security                 String    @default("")
  notes                    String    @default("")
  driver_name              String    @default("")
  driver_phone             String    @default("")
  butler_name              String    @default("")
  butler_phone             String    @default("")
  security_contact       String    @default("")
  emergency_contact        String    @default("")
  yacht                    String    @default("")
  jet                      String    @default("")
  restaurant_reservations  String    @default("")
  club_reservations        String    @default("")
  host_name                String    @default("Matthieu Dubourg")
  host_phone               String    @default("+1 332 733 9543")
  host_contact             String    @default("")
  tailored_for             String    @default("")
  follow_up_status         String    @default("follow_up")
  payment_status           String    @default("pending")
  total_amount             String    @default("")
  amount_received          String    @default("")
  payment_method           String    @default("")
  payment_notes            String    @default("")
  event_booking            String    @default("")
  event_venue              String    @default("")
  created_at               DateTime  @default(now())
  updated_at               DateTime  @updatedAt
  client                   Client?   @relation(fields: [client_id], references: [id], onDelete: SetNull)
  days                     TripDay[]
  checklist_items          TripChecklistItem[]
  stay_closing             StayClosing?

  @@index([client_id], map: "idx_trips_client")
  @@map("trips")
}

model TripChecklistItem {
  id            Int    @id @default(autoincrement())
  trip_id       Int
  category      String
  title         String @default("")
  status        String @default("todo")
  notes         String @default("")
  due_date      String @default("")
  reminder_date String @default("")
  sort_order    Int    @default(0)
  trip          Trip   @relation(fields: [trip_id], references: [id], onDelete: Cascade)

  @@index([trip_id], map: "idx_checklist_trip")
  @@map("trip_checklist_items")
}

model TripDay {
  id         Int        @id @default(autoincrement())
  trip_id    Int
  date       String
  sections              String     @default("[]")
  destination_override  String     @default("")
  trip                  Trip       @relation(fields: [trip_id], references: [id], onDelete: Cascade)
  activities Activity[]

  @@unique([trip_id, date])
  @@index([trip_id], map: "idx_trip_days_trip")
  @@map("trip_days")
}

model Activity {
  id             Int     @id @default(autoincrement())
  trip_day_id    Int
  period         String
  activity_type  String  @default("activity")
  time           String  @default("")
  title          String  @default("")
  details        String  @default("")
  status         String  @default("confirmed")
  booking_status String  @default("to_request")
  assigned_to    String  @default("")
  booking_notes      String  @default("")
  sort_order         Int     @default(0)
  establishment_city String  @default("")
  beach_sunbeds      Boolean @default(false)
  beach_sunbeds_time String  @default("")
  beach_lunch        Boolean @default(false)
  beach_lunch_time   String  @default("")
  beach_sunbeds_status String @default("to_request")
  beach_lunch_status   String @default("to_request")
  transport_type       String @default("")
  transport_pickup     String @default("")
  transport_destination String @default("")
  trip_day           TripDay @relation(fields: [trip_day_id], references: [id], onDelete: Cascade)

  @@index([trip_day_id], map: "idx_activities_day")
  @@map("activities")
}

model Establishment {
  id             Int      @id @default(autoincrement())
  name           String
  category       String
  city           String   @default("")
  address        String   @default("")
  contact_name   String   @default("")
  phone          String   @default("")
  whatsapp       String   @default("")
  email          String   @default("")
  website        String   @default("")
  instagram      String   @default("")
  notes          String   @default("")
  price_level    String   @default("")
  tags           String   @default("")
  internal_notes String   @default("")
  is_favorite    Boolean  @default(false)
  commission_available           Boolean @default(false)
  commission_calc_type           String  @default("percentage")
  commission_percentage          String  @default("")
  commission_fixed_amount          String  @default("")
  commission_calc_custom           String  @default("")
  commission_basis                 String  @default("total_bill")
  commission_basis_custom          String  @default("")
  commission_eligibility           String  @default("none")
  commission_eligibility_custom    String  @default("")
  commission_threshold_amount      String  @default("")
  seasonal_commission_enabled      Boolean @default(false)
  seasonal_commission_start        String  @default("")
  seasonal_commission_end          String  @default("")
  seasonal_commission_target       String  @default("")
  seasonal_commission_after_target Boolean @default(true)
  created_at     DateTime @default(now())
  updated_at     DateTime @updatedAt
  stay_closing_entries StayClosingEntry[]

  @@index([category], map: "idx_establishments_category")
  @@index([city], map: "idx_establishments_city")
  @@index([name], map: "idx_establishments_name")
  @@map("establishments")
}

model StayClosing {
  id         Int                @id @default(autoincrement())
  trip_id    Int                @unique
  closed_at  DateTime           @default(now())
  vip_notes  String             @default("")
  created_at DateTime           @default(now())
  updated_at DateTime           @updatedAt
  trip       Trip               @relation(fields: [trip_id], references: [id], onDelete: Cascade)
  entries    StayClosingEntry[]

  @@index([trip_id], map: "idx_stay_closings_trip")
  @@map("stay_closings")
}

model StayClosingEntry {
  id                      Int            @id @default(autoincrement())
  stay_closing_id         Int
  establishment_id        Int?
  establishment_name      String
  activity_ids            Json           @default("[]")
  approximate_total_bill  String         @default("")
  food_amount             String         @default("")
  premium_drinks_amount   String         @default("")
  internal_notes          String         @default("")
  calculated_commission   String         @default("")
  commission_applied      Boolean        @default(false)
  commission_received              Boolean        @default(false)
  commission_received_at           String         @default("")
  commission_pending_season_target Boolean        @default(false)
  commission_summary               String         @default("")
  created_at              DateTime       @default(now())
  updated_at              DateTime       @updatedAt
  stay_closing            StayClosing    @relation(fields: [stay_closing_id], references: [id], onDelete: Cascade)
  establishment           Establishment? @relation(fields: [establishment_id], references: [id], onDelete: SetNull)

  @@unique([stay_closing_id, establishment_name], map: "stay_closing_entries_closing_name_key")
  @@index([stay_closing_id], map: "idx_stay_closing_entries_closing")
  @@map("stay_closing_entries")
}

model ConciergeEvent {
  id             Int          @id @default(autoincrement())
  name           String
  category       String
  destination    String
  start_date     String       @default("")
  end_date       String       @default("")
  contact_name   String       @default("")
  phone          String       @default("")
  whatsapp       String       @default("")
  email          String       @default("")
  website        String       @default("")
  notes          String       @default("")
  internal_notes String       @default("")
  is_favorite    Boolean      @default(false)
  created_at     DateTime     @default(now())
  updated_at     DateTime     @updatedAt
  venues         EventVenue[]

  @@index([category], map: "idx_events_category")
  @@index([destination], map: "idx_events_destination")
  @@index([name], map: "idx_events_name")
  @@map("concierge_events")
}

model EventVenue {
  id             Int             @id @default(autoincrement())
  event_id       Int?
  name           String
  destination    String
  contact_name   String          @default("")
  phone          String          @default("")
  whatsapp       String          @default("")
  email          String          @default("")
  website        String          @default("")
  notes          String          @default("")
  internal_notes String          @default("")
  is_favorite    Boolean         @default(false)
  created_at     DateTime        @default(now())
  updated_at     DateTime        @updatedAt
  event          ConciergeEvent? @relation(fields: [event_id], references: [id], onDelete: SetNull)

  @@index([event_id], map: "idx_event_venues_event")
  @@index([destination], map: "idx_event_venues_destination")
  @@index([name], map: "idx_event_venues_name")
  @@map("event_venues")
}

model AppUser {
  id            Int                  @id @default(autoincrement())
  email         String               @unique
  name          String
  role          String               @default("admin")
  password_hash String
  created_at    DateTime             @default(now())
  updated_at    DateTime             @updatedAt
  credentials   WebAuthnCredential[]

  @@map("app_users")
}

model WebAuthnCredential {
  id              Int      @id @default(autoincrement())
  user_id         Int
  credential_id   String   @unique
  public_key      Bytes
  counter         BigInt   @default(0)
  device_type     String   @default("")
  backed_up       Boolean  @default(false)
  transports      String   @default("")
  created_at      DateTime @default(now())
  user            AppUser  @relation(fields: [user_id], references: [id], onDelete: Cascade)

  @@index([user_id], map: "idx_webauthn_user")
  @@map("webauthn_credentials")
}
